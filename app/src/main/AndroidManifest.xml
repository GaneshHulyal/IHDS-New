<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="in.ganeshhulyal.aidatalab">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:largeHeap="true"
        android:hardwareAccelerated="false"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AIDataLab"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.UploadHumanAgreement"></activity>
        <activity android:name=".activities.UploadAgreement" />
        <activity android:name=".activities.MultipleImageUpload" />
        <activity android:name=".activities.CameraUploadActivity" />
        <activity android:name=".activities.MetaDataNonHumanCentric" />
        <activity android:name=".activities.MetaDataHumanCentric" />
        <activity android:name=".activities.ClassName" />
        <activity android:name=".activities.UploadActivity" />
        <activity android:name=".activities.HumanCentricAgreement" />
        <activity android:name=".activities.UserMenu" />
        <activity android:name=".activities.ImageUploadNonHumanCentric" />
        <activity android:name=".activities.AgreementActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.UserRegistration" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>